{
  "name": "server",
  "scripts": {
    "dev": "PORT=3001 bun run --hot src/index.ts",
    "worker:git": "bun run --hot src/worker/git-extract-worker.ts",
    "clear:worker:extractor": "bun run --hot src/clear-queue.ts"
  },
  "dependencies": {
    "dotenv": "^17.2.1",
    "hono": "^4.8.10",
    "zod": "^4.0.13"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}
