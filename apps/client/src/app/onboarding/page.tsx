import OnboardingForm from "@/__components-app/onboarding-form";

// add metadata here now
const metadata = {
  title: "Onboarding",
  description: "Onboarding page for new users",
};

const page = () => {
  return <OnboardingForm />;
};

export default page;
